package com.point2;

import java.math.BigDecimal;

public class CreditCardPayment implements Payment {

	public boolean pay(BigDecimal amount) {
		// requests the necessary data and performs the operation
		// returns true if the operation is performed and otherwise returns false
		return true;
	}

}
